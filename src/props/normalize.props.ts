export interface NormalizeProps {
	isNormalize?: boolean;
	hasDots?: boolean;
	isDisabled?: boolean;
	isInteractable?: boolean;
	isHidden?: boolean;
	isGrow?: boolean;
	isPointer?: boolean;
	isSans?: boolean;
	isSerif?: boolean;
}
