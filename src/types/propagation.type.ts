export enum PropagationActionType {
	FOCUS = 'focus',
	CLICK = 'click',
}

export enum PropagationTagType {
	DIV = 'div',
	INPUT = 'input',
	SPAN = 'span',
	BUTTON = 'button',
}

export enum PropagationPriorityType {
	VERY_HIGH = '5',
	HIGH = '4',
	MEDIUM = '3',
	LOW = '2',
	VERY_LOW = '1',
}

export enum PropagationPropType {
	PROPAGATION_ACTION = 'propagation-action',
	PROPAGATION_PRIORITY = 'propagation-priority',
}
