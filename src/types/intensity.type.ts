export enum IntensityType {
	WEAKEST = 'weakest',
	WEAK = 'weak',
	MAIN = 'main',
	STRONG = 'strong',
	STRONGEST = 'strongest',
}

export interface IntensityKeyType {
	weakest: string;
	weak: string;
	main: string;
	strong: string;
	strongest: string;
}
