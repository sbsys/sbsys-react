export enum SerieType {
	BASE = 'base',
	PRIMARY = 'primary',
	SECONDARY = 'secondary',
	TERTIARY = 'tertiary',
	ACCENT = 'accent',
	INFO = 'info',
	SUCCESS = 'success',
	WARNING = 'warning',
	DANGER = 'danger',
}

export interface SerieKeyType {
	base: string;
	primary: string;
	secondary: string;
	tertiary: string;
	accent: string;
	info: string;
	success: string;
	warning: string;
	danger: string;
}
